<div class="container">
  <div class="row">

    <ul class="nav nav-pills justify-content-center categories">
      <li role="presentation" class="nav-item"><%= link_to "全部餐廳", root_path, class: "active nav-link" %></li>
      <% @categories.each do |category| %>
        <li role="presentation" class="nav-item"><%= link_to category.name, category_path(category), class: "nav-link" %></li>
      <% end %>
    </ul>

    <br>

    <%= render partial: "shared/restaurant_list" %>
    
  </div>
</div>